---
- name: Independently validate hardware state
  hosts: ai_hosts
  gather_facts: true
  become: true
  tags:
    - validation
    - hardware
  roles:
    - role: hardware_inventory
      tags: [validation, inventory, hardware]
    - role: hardware_validation
      tags: [validation, hardware]
