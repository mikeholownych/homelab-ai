---
- name: Export normalized CMDB facts and configuration item data
  hosts: ai_hosts
  gather_facts: true
  become: true
  tags:
    - facts-export
    - cmdb
    - operations
  tasks:
    - name: Run CMDB export role
      ansible.builtin.include_role:
        name: cmdb_export
      tags: [cmdb, facts-export]
